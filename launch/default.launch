<!-- ROS launch file -->
<launch>
    <arg name="node_name"       default="mavros_mission" />
    <arg name="launch_prefix"   default="xterm -hold -geometry 105x19 -e" />

    <!-- the node -->
    <node name="$(arg node_name)" pkg="ros_mavros_wp_mission" type="ros_mavros_wp_mission" launch-prefix="$(arg launch_prefix)">
        <!-- remapping -->

    </node>
</launch>
